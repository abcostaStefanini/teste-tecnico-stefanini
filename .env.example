# Variáveis usadas pelo docker-compose. Copie para `.env` e ajuste se necessário.
# Os valores abaixo são os defaults já aplicados no docker-compose.yml.

# PostgreSQL
POSTGRES_DB=taskdb
POSTGRES_USER=taskuser
POSTGRES_PASSWORD=taskpass
